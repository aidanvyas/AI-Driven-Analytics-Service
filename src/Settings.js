import React from 'react';
import { Box } from '@chakra-ui/react';

const Settings = () => {
  return (
    <Box>
      <h1>Settings</h1>
      <p>This is where users can configure their preferences and account settings.</p>
    </Box>
  );
};

export default Settings;
